import * as FileSystem from 'expo-file-system';

export const Database = {
    DATABASE_NAME : 'database.db',
    DATABASE_VERSION : 1,
    DATABASE_PATH : `${FileSystem.documentDirectory}SQLite/`,

}


